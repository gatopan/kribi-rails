<nav class="navbar-default navbar-static-side" role="navigation">
  <div class="sidebar-collapse">
    <ul class="nav metismenu" id="side-menu">
      <li class="nav-header">
        <div class="dropdown profile-element">
          <span>
            <img alt="image" class="img-circle" src="<%= image_url 'profile_small.jpg' %>"/>
          </span>
          <a data-toggle="dropdown" class="dropdown-toggle" href="#">
            <span class="clear">
              <span class="block m-t-xs">
                <strong class="font-bold">David Williams</strong>
              </span>
              <span class="text-muted text-xs block">
                Art Director <b class="caret"></b>
              </span>
            </span>
          </a>
          <ul class="dropdown-menu animated fadeInRight m-t-xs">
            <li><a href="#">Settings</a></li>
            <li><a href="#">Permissions</a></li>
            <li><a href="#">Logout</a></li>
          </ul>
        </div>
        <div class="logo-element">
          <%# <%= image_tag 'catbread.gif' %>
          KPPC
        </div>
      </li>

      <li class="<%= is_active_controller('static_pages') %>">
        <a href="/"><i class="fa fa-th-large"></i> <span class="nav-label" data-i18n="nav.dashboard">Home</span> <span class="fa arrow"></span></a>
      </li>

      <li class="<%= is_active_batch %>">
        <a href=""><i class="fa fa-th-large"></i> <span class="nav-label" data-i18n="nav.dashboard">Batches</span> <span class="fa arrow"></span></a>
        <ul class="nav nav-second-level collapse <%= is_active_batch %>">
          <% @batch_routes.each do |batch_route| %>
            <li class="<%= is_active_route(batch_route) %>"><%= link_to batch_route.fetch(:resource_name), batch_route.fetch(:resource_path) %></li>
          <% end %>
        </ul>
      </li>

      <li class="<%= is_active_event %>">
        <a href=""><i class="fa fa-th-large"></i> <span class="nav-label" data-i18n="nav.dashboard">Events</span> <span class="fa arrow"></span></a>
        <ul class="nav nav-second-level collapse <%= is_active_event %>">
          <% @event_routes.each do |event_route| %>
            <li class="<%= is_active_route(event_route) %>"><%= link_to event_route.fetch(:resource_name), event_route.fetch(:resource_path) %></li>
          <% end %>
        </ul>
      </li>
    </ul>
  </div>
</nav>
